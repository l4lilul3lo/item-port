// being called in getproducts
const sifter = (query) => {
  // get products by limit if provided, otherwise limit default.
  // if query.filterBy
  // take limited products and filter them
  // if query.sortBy
  // take limited products and sort them
  // limit
};
